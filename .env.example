# Copy this file to `.env.local` (gitignored) and fill in your own values.
#
# Important: this project can read/write files and run commands on your machine.
# Always set ADMIN_TOKEN and restrict ALLOWED_WORKSPACE_ROOTS.

# ---- Core (recommended) ----
# Required for write APIs (never commit your real token)
ADMIN_TOKEN=

# Restrict where workspaces can be registered (semicolon/comma separated).
# Example (Windows): C:\projects;D:\work
# Example (Linux/macOS): /home/me/projects,/mnt/work
ALLOWED_WORKSPACE_ROOTS=

# Optional server settings
# HOST=0.0.0.0
# PORT=18787

# Optional safety switch: when true, write endpoints reject requests even with ADMIN_TOKEN
# READ_ONLY_MODE=false

# Optional storage settings
# DB_PATH=./data/app.sqlite
# RUNS_DIR=./runs

# ---- Push notifications (PushPlus -> WeChat) (optional) ----
# PushPlus token (required to enable notifications)
PUSHPLUS_TOKEN=

# Enable/disable notifications (optional).
# If omitted, notifications will auto-enable when PUSHPLUS_TOKEN is set.
# PUSH_NOTIFICATIONS_ENABLED=true

# Optional: override PushPlus endpoint (useful for tests / self-hosted proxy)
# PUSHPLUS_ENDPOINT=https://www.pushplus.plus/send

# Optional: PushPlus channel/template
# - wechat: WeChat official account
# - app: PushPlus App notifications
# PUSHPLUS_CHANNEL=wechat
# PUSHPLUS_TEMPLATE=markdown

# Optional: include a clickable URL in the push message (e.g. your VPN/Tailscale URL)
# PUSH_BASE_URL=http://100.x.y.z:18787

# What to notify (all optional)
# PUSH_NOTIFY_RUN_FINAL=true
# PUSH_NOTIFY_RUN_STEP=true
# PUSH_NOTIFY_ASK_REPLY=true
