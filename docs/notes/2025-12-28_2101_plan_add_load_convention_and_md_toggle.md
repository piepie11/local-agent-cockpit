# 2025-12-28 21:01 — `plan.md` 增补“Load convention + Markdown 渲染开关”与 UX 约束

## 背景

- 计划/约定文件是用户理解与执行的入口，但目前计划里对“约定加载入口”与“Markdown 渲染可切换”的需求描述不够明确。
- 同时需要在工程约定中明确：任何修改都要兼顾 Windows/Linux 与移动端体验，避免再次出现“手机端不好用/交互被刷新打断”的问题。

## 变更

- `plan.md`：
  - 在 M3（workspace 可编辑）中补充 UI 任务：在 “Load plan” 旁新增 “Load convention” 按钮，并在加载窗口加入 “Markdown 渲染” 开关（默认开启，关闭按纯文本显示）。
- `约定.md`：
  - 新增 7.5 “用户体验与移动端适配（强制）”，要求所有修改默认兼容 Windows/Linux，并且涉及 Web UI 的改动要验证桌面端与移动端。

## 验证

- 文档一致性检查：确认 `plan.md` 的 M3 里包含上述任务点，`约定.md` 含 7.5 小节。

