# 2025-12-25 20:19 — 放宽 Manager 提示词（允许临场决断/发散，但强制留痕）

## 目的
- Manager 需要具备“临场决断能力”：当执行中发现更合理路径或计划不适配时，允许调整顺序/拆分/补充必要子任务。
- 仍保持可控：Executor 不发散；所有偏离计划的决策必须可追溯，并严格走 git 闭环。

## 变更
- `prompts/manager_system.md`：
  - 明确 `plan.md` 是基线而非枷锁：允许基于证据做临场调整。
  - 增加“偏离必须留痕”的要求：偏离计划时必须在 `docs/notes/` 单独写决策记录并提交。
  - 仍保持输出契约：未完成必须 `<MANAGER_PACKET>`，完成必须严格 `Done`。

## 验证
- 手工检查：提示词改动不影响解析协议（`<MANAGER_PACKET>` / `Done` 约束未变）。

