# M0-2 Roundtrip（Manager → Executor → Manager(Done)）

## 做了什么

- 新增 `prompts/manager_system.md` 与 `prompts/executor_system.md` 作为最小 prompt 资产
- 新增 `scripts/m0_roundtrip.js`：最小回合编排脚本（Manager→Executor→Manager），并校验最终必须输出 `Done`
- 更新 `package.json`：增加 `m0:roundtrip` 脚本入口

## 环境信息

- node: v22.16.0
- codex-cli: 0.77.0
- OS: Windows 11
- cwd: `E:\sjt\others\auto_codex`

## 验证命令

```bash
npm run m0:roundtrip
```

## 结果

- PASS：最终 Manager 输出严格等于 `Done`
- 本次 run 目录：`runs/roundtrip-20251224_020135/`（目录本身不纳入 git）

## 下一步

- 进入 M1：实现 Web Server + SSE + SQLite（node:sqlite），把 roundtrip 的回合流在网页上可视化并可回放/导出

