# M2.3 doc writer convention template

## Summary
- Added a generic convention template file for doc writer use.
- Inject convention template into docKind=convention prompt with env-path override and fallback order.

## Verification
- npm test

## Results
- npm test: PASS

## Next
- Consider template source overrides in config UI if needed.
